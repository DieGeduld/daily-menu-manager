import { createApp } from 'vue'
//import DailyMenuApp from './components/DailyMenuApp.vue';

//import CommonComponents from './common/components';

//import { formatPrice, formatTime } from './common/utils/formatters';

//import { i18nPlugin } from './plugins/i18n'
import '@/src/sass/admin.scss'

// Create Vue app when DOM is ready
// document.addEventListener('DOMContentLoaded', () => {
//   const menuContainers = document.querySelectorAll('.daily-menu-manager');
  
//     menuContainers.forEach((container) => {

//         const app = createApp(DailyMenuApp, {
//             menuId: menuOrderForm.querySelector('input[name="menu_id"]').value
//         })
        
//         // Mount the app
//         app.mount('#menu-order-form')

//     });
// })
